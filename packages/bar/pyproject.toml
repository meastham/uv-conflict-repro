[project]
name = "bar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Eastham", email = "mike.eastham@databricks.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy~=2.0",
]

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
